# TaleForge

一台**剧本机器**：底座只保证结构永不坏，一切能力（机制、工艺）都是货架上的可选件，剧本用"声明选件 + 设定数据"换来一局稳定成立的游戏。基于 DeepSeek Harness（dsh）作 AI 运行时内核；玩家前端自写且固定，所有剧本共用同一 UI。单人自用，Docker 部署。

## 平台定义（2026-08-19 拷问定稿，覆盖 08-18 版；不再返工）

三层结构与归因规则：

- **底座**（平台保证，坏了就是平台 bug）：输出契约、机制与进度的固定流程、受控 Markdown 语法边界、数值裁决代码权威、转幕与结局代码判定、戏内铁律与场外协议。
- **货架**（平台提供，剧本自由选用；上新货不改剧本格式、不改前端框架）：机制模块 v1 四件套（判定/资源条/属性表/物品栏）+ 经验等级（progression，2026-08-21：GM 报经验、代码按阈值算等级发点、玩家在卷宗加点随下一步行动由 GM 经 spend_points 原样落账——dsh 没有绕过回合写事件的 RPC，玩家侧状态变更只能搭回合）+ 工艺模块（standard/shuang/harem/hardcore）。结构化战斗（先攻/回合/敌人实体）不做。机制 UI 是固定通用组件集：状态侧栏 + 叙事流内结构化卡片；**显示位置由平台枚举（strip/panel/hidden）、剧本只做选位**，分组标题可声明——剧本不能定义新组件、不碰布局与样式。
- **剧本**（绝对自由）：设定数据 + 显式声明选件 + 自带工艺文本（无条数上限）。**无隐藏默认**——不声明就没有；剧本里写了什么，GM 就背了什么。
- **强度分层**（2026-08-20 用户拍板）：工艺模块只管"怎么写"，**rating 是"写多深"的唯一契约**——模块永不设强度上限或下限。作者个人的创作边界属于作者层（谁写剧本谁自己把握），不得写进平台货架；实例：harem 模块曾夹带"收在余韵"的强度线，与剧本 rating 每回合对拉，已中立化。
- **归因**：结构坏 = 平台 bug；味道差 = 剧本的声明或自带文本；货架件本身不行 = 平台库 bug。

判定分工（Rpgforge 24 轮收敛出的分工表，照走，不设第三个裁判）：

- 能机械判定的归**代码权威**：数值裁决、转幕、结局。GM 只承接叙事，失败只能"否，但…"缓冲，不能翻案。
- 语义判定由 **GM 本人在固定流程里表态**（它是剧情的作者，不存在更知情的裁判）：锚点达成走每回合强制上报；防漏报靠工具返回值每回合列出未完成锚点与完成信号 + 代码数停滞回合分档加压。
- 剧情合理性与成败裁量是 GM 本职；剧情总结交 dsh 自带压缩，结构状态（数值/进度）每回合随工具返回值回注，不依赖总结存活。

修订机制（设定中途可改）：

- 一切剧本声明都只是**初始种子**：修订是事件（走 tool/result.meta），折叠出"现行有效设定"，每回合回注 GM，效力高于剧本原文。进度/机制投影从设计起就按"可修订"建，不做成编译期写死。
- 修订只对未来生效；想改已发生的剧情 = fork 回到那个回合。修订积累后可"落盘"合并回 story.json 源。
- 剧本永远只有**一个现行正式版**：编译、导出、新开局都用它；历史版本靠 git 留档，源文件不开分支。

戏内/场外：

- **戏内铁律**（底座）：正戏永不谈机制、不解释剧情安排、不跳出叙述者身份；识别到场外话时固定动作只有一行短提示指向场外，然后场景保持、重发行动块。
- **场外通道**：同会话同一个 GM，消息以【场外】开头；GM 以主持人身份答疑、接受软指令（剧情走向/人物戏份/风格）、执行结构化修订；场外回合不推进剧情、不产出行动块、不调数值工具。

多 agent 边界：多 agent 协同只进工坊（造剧本线，玩家不等待），**永不进玩家回合**——护栏 1 的正面表述。

已知弱点（如实记录，不装作已解决）：

1. GM 自报锚点是全设计唯一剩余的 LLM 信任点：虚报会被代码权威锁死。缓冲：锚点必须写具体完成信号 + fork 救档。F4 验收专测此处。
2. 味道层无护栏是设计而非缺陷：平台验收通过 ≠ 游戏好玩，后者靠内容线自己挣。
3. 验收整局是抽样烟测；长期保障靠被动观测逐局累积。
4. 契约用四件同形状机制定的，将来异形机制可能需要契约 v2，不追求一次定终身。

## 地基计划（F1–F6，当前阶段）

- **F1 契约定稿**：story 格式升 v1（craft 段显式声明、锚点完成信号）、persona 底座收缩 + 工艺模块化、创作说明书（兼平台能力契约）。F1 完成即交付《吞噬进化》重制版详尽初稿 + 说明书——整个剧本文件夹即外发包，用户交外部 AI 打磨，两线并行。
- **F2 幕进度 + 结局**：进度上报工具（与 adjust_resources 同一条机械规则：每回合必调、无变化传空）、投影折叠、压力分档、剧终卡与重新开始；场外 UI 入口；修订工具（设定类）。
- **F3 机制三件补齐**：判定、属性表、物品栏，复制资源条已打通的链路；机制定义可修订。
- **F4 验收（2026-08-19 完成）**：被动结构观测（纯函数逐回合检查，只写日志，不做面板）+ 存档折叠回归（scripts/refold.ts）+ 基准剧本（presets-dev/baseline，不部署）本地整局跑通。实测：13 回合开场到剧终、2 次转幕、结局机制全对、判定/资源/物品真实触发、场外修订生效并抓出 lossless bug；属性被判定引用但调整未触发（条件未出现，引擎与资源同源有单测）。原"跨一次上下文压缩"标准取消：1M 窗口下正常局长不可达（13 回合约 4 万 token），结构状态本就逐回合随工具回注、不依赖压缩存活；压缩品质留给真实超长局观测。
- **F5 工坊（2026-08-19 完成）**：访谈→publish_story（校验失败逐条退回、agent 自修重发）→编译→剧本库立即可玩，E2E 实测含一次真实自修循环；修订落盘（卷宗按钮→合并回源→重编译，下一局生效）。剧本源改双根：仓库 presets/ 是内置种子，数据卷 scenarios/ 是用户内容（工坊产出、落盘产物），同 id 数据卷覆盖。工坊常驻单会话与游戏存档并存（单存档清理保住它）。多 agent 协同暂未启用——单 agent 访谈已闭环，需要时再加。
- **F6 部署**：随 F2–F5 分批完成（每批验证后即上线），无需一次性切换。

地基期间服务器与现役存档原封不动，开发验证全在本地 dsh。"先打地基"限定为玩家运行时能力，与护栏 4 不冲突——上次死于给作者造工具，这次补的是玩家脚下的地板。

## 单存档（当前阶段的产品约束）

平台同时只保留一个进行中的会话：BFF 只暴露最近一个玩过的，开新局时物理删除其他会话日志（dsh 没有删除 RPC）。分支功能因此关闭——接口返回 `single-save-mode`，前端无入口（"重写上一回合"内部用 fork 实现，不算分支）。

词汇与界面归属（2026-08-20 用户定稿）：进行中的游戏叫**会话**，快照叫**存档**（存档=快照进数据卷 `save-backups/`，读档=拷回原位成为唯一会话）。主页只保留"继续冒险"一行 + 剧本卡；会话与存档的全部管理（存档/读档/删除）都在剧本详情页，按剧本过滤显示。剧本源全部住数据卷 `scenarios/`（仓库 presets/ 种子机制保留但当前为空），删剧本=删源+删编译产出+**级联删除它的存档与修改对话**，有会话正玩着时 409 拒绝。

修改剧本的唯一入口是**详情页唤起 GM**（"去工坊改"方案被用户否决）：每个剧本各自常驻一个修改对话（workshop preset，同一聊天组件换标题与开场白），映射存 `DSH_HOME/edit-sessions.json`。两条铁律：**所有 pruneSessions 调用点必须保住映射里的全部会话**（走 protectedSessions，宁多保不误删）；判活不能只按磁盘即时裁决——session.create 返回与目录落盘之间有延迟，实测"建完立刻查磁盘"会把刚建的映射误判失效，dsh 内存里仍是 blank 的会话要视为活着（editSessionAlive）。常驻工坊会话的定位要排除修改对话（同为 workshop preset，按映射区分）。

**在服务器上验证功能时复用同一个测试会话，或验证完立刻清理。** 曾经一次改提示词验证一轮，累积 12 个存档全留在用户的列表里，用户无从分辨哪个是哪个。

## 硬护栏（Rpgforge 教训，违反即返工）

1. 玩家等待路径上只有 GM 主循环一个 LLM 环节（含其工具调用续写步）；Director/Observer/Judge 类额外 LLM 层一律不加。
2. GM prompt 以正向工艺指令为主，硬性禁令保持个位数；输出不满意时改工艺描述，不堆规则。
3. 状态一律 id 引用 + 严格 schema + 纯 upsert；不写模糊文本匹配兜底。
4. 玩家循环好玩之前不做作者工具面板/观测面板/评测体系；调试观测用 dsh 自带 Web 工作台（loopback 直连 3090）。可视化剧本编辑器永不做。被动观测只写日志，永不长成面板。
5. 文档只留本文件 + README + 剧本创作说明书；历史与过程交给 git log。
6. 主动跑的 LLM 评估设施（judge/golden replay 类）不建——Rpgforge 实证其使用次数≈0；观测一律被动、确定性。

## 部署（唯一目标环境）

- 生产部署永远只在**唯一一台目标服务器的 Docker** 里（主机别名、部署目录、deploy key 等真实值见本机私有的 `DEPLOY.local.md`，已 gitignore）。不部署到其他任何环境；本地只做开发与验证。
- 该机 Docker 命令需 `sudo`（部署用户不在 docker 组）。
- 对外端口 **31415**，**绑定 127.0.0.1**：BFF 与 dsh 都没有认证，公网直接暴露等于把 API key 和存档开放给所有人。远程访问走 SSH 隧道或在 lucky 上加认证的反向代理。
- 端口须低于 32768：该机临时端口范围是 32768–60999，监听端口落在其中会偶发绑定冲突。
- 容器内 dsh 与 BFF 必须同进程空间（dsh 只信任 loopback），单容器双进程由 `docker/entrypoint.sh` 拉起。
- 持久化只有一个卷：宿主机的 `<部署目录>/data` → 容器内 `DSH_HOME`，装着全部存档、凭据、编译产出的剧本 preset，以及 `scenarios/`（用户内容：工坊产出与修订落盘的剧本源，编译时覆盖仓库同 id 种子）。重建容器安全，删卷即丢档。
- **API Key 由 WebUI 设置页写入**，经 dsh credentials 服务落到 `data/.credentials.yaml`，热生效、随卷持久化。`.env` 里保持没有 `DEEPSEEK_API_KEY`：环境变量是只读层，一旦有非空值就遮蔽写入通道，设置页会变成只读（此时 `credentials.describe` 返回 `writable: false`）。
- 服务器用只读 deploy key 拉取，key 路径写进服务器仓库的 `core.sshCommand`，`git pull` 免密（具体路径见 `DEPLOY.local.md`）。
- 更新流程：本地推送 → SSH 到目标服务器 → `cd <部署目录> && git pull && sudo docker compose up -d --build`。依赖层有缓存，仅改应用代码时重建很快。

## GM 提示词的分层（packages/scenario-compiler/src/persona.ts）

顺序是：**底座（结构保证）→ 工艺模块（剧本声明选用）→ 剧本数据 → 输出契约 → 开局指令**。

- 分层顺序即 prefix cache 的共享范围：改底座打散全部剧本的缓存，改某个工艺模块打散选用它的剧本的。
- **输出契约必须排在最末**，紧邻生成点。它原本跟在叙事工艺后面，中间隔着工艺模块与整份剧本设定，实测模型写到结尾会漏掉行动块，玩家因此没有选项可点。已有测试锁住这个顺序，重构时别挪回去。
- **工具返回值比 persona 更贴近生成点**：回合固定流程引入两次工具调用后，契约又被挤远了——实测开场回合漏行动块（修复前 2 局 1 翻车，修复后 3/3 通过）。行动块提醒已作为固定尾行写进 report_progress 简报（packages/progress renderBrief），动简报时别删。要 GM 在"写正文的那一步"稳定做某事，把指令放进最后一个工具的返回文本里，比放 persona 任何位置都有效。
- **挂在工具返回值上的提醒有个致命盲区：工具没被调用，提醒通道整个断流**。实测 37 回合整局里 6 个强场面回合（大战、双修、情感高潮）完全跳过固定流程——文学惯性压倒流程指令，而此时没有任何提醒在场。对策是回合头机械注入：BFF 给每条正戏玩家消息追加【回合流程】文本块（apps/bff prompt 路由），回合开头就贴住生成点，不依赖 GM 自觉；前端按前缀隐藏。修订类语义变更还要联动边界——"双修回满"落账后被旧 maxStep=25 卡死，revise_setting 现已自动提醒同步改边界。
- 底座只写结构；一切"怎么写得好"都在工艺模块或剧本自带文本里。同类写法在多个剧本重复出现时沉淀成新模块，别各写各的。

## 工艺指令的篇幅要匹配内容占比

给 GM 的工艺指令写在哪里，GM 就在哪里发力；没写指令的部分，无论剧本设定多丰满，都会退化成流水账。

实测教训：剧本定的配比是"情感为主、丧尸是舞台"，但爽文模板里七条指令全是碾压、捧场、正反馈，关于关系与张力的一条都没有。结果是好感度数字照涨、剧情照推，玩家的评价却是"没啥成人元素，只是同行的都是女人而已"——占八成内容的部分没有任何工艺指导。

写工艺模块或剧本 craft 段前先问：这部作品的回合时间花在哪儿？指令就得压在那儿。

## 单存档模式下不能随手在服务器上验证

单存档是物理删除实现的：**新建会话会直接删掉用户正在玩的存档**。要跑端到端验证，先确认用户当前没有在玩的进度，或者征得同意；需要反复验证时在本地起 dsh，别动服务器。

## 长局文风漂移与剧本贴身提醒（craft.reminder）

proximity 链的最后一环：**长会话里，正文自己的先例比 persona 里任何声明都更贴近生成点**。实测（2026-08-20《唯一男性异能者》41 回合局）：rating 词表完整在场、GM 推理里都复述"露骨直呼"，落笔却跟着前 30+ 回合的含蓄先例走，全程规避词表；干净上下文里同一模型同一 persona 首回合就照写（14 处命中）。文风一旦定调即自锁，GM 场外口头认错两次、正文不变。对策是货架位 craft.reminder（≤600 字）：剧本自声明的短提醒，BFF 拼进每回合的【回合流程】注入块贴住生成点；内容全由剧本定，平台不携带强度（强度分层不破）。BFF 每回合现读 preset 里的 story.json——修改剧本重新发布后，**进行中的局下一回合就吃到新提醒**，不受会话锁定影响。

分幕变体 acts[].reminder：文风/世界状态随剧情阶段变化的剧本，每幕可声明自己的提醒**替换** craft.reminder 注入（BFF 从 progress 投影取 actIndex 现挑现注）；只注当前幕那段，未到的幕天然防剧透，没写的幕回落全局。语义是替换不是叠加——序幕"正常世界不写底噪"与全局"每回合必须有底噪"这类互斥要求叠加会自相矛盾。

## 平台边界的提示词要连同出路一起写

agent 只知道"这里不行"而不知道正确出路时，会向玩家把工具边界误报成平台边界。实测（2026-08-20《唯一男性异能者》局）：玩家局内要求新增「欲望」数值条，场外 GM 只从 revise_setting 描述里读到"不支持中途增删条目"，于是断言"平台没法挂进度条"——而正确出路（详情页「修改剧本」把条目写进源、新开局生效）它无从知道，旧拒绝理由里写的"走落盘+新局"甚至是错的（落盘合并的是修订，修订本身就加不了条目）。已修：revise_setting 的描述与拒绝理由、工坊 persona 的"修改已有剧本"节，都写明了增删条目的正确路径。规矩：凡工具描述或拒绝理由出现"不支持/不允许"，同一句话必须给出正确路径。

## 回合头注入带面板快照（GM 的账实相符靠它）

机制状态折叠在会话事件里，长局中初始清单早被上下文稀释，GM 会**在正文里发明装备**。实测（2026-08-20 v2 局 35 回合）：物品栏躺着开局的一截钢管，GM 从第 6 回合起让主角抡一把从没入账的折叠椅连抡三回合；唐苓的菜刀第 11 回合递出、第 16 回合才补账；双修的属性成长要等玩家场外催（第 21 回合）才补。对策：turnHeadBlock 每回合经 `session.history {maxMessages:1}` 取全量投影值（该 RPC 直接返回 projections.values，一次拿全 mechanics/attributes/inventory/progress，不必拉整段历史），拼成【当前面板】贴住生成点，附"账实相符+当回合结算"指令。hidden 资源一并给 GM——注入块玩家侧被前端按【回合流程】前缀整块隐藏。

同局也确认了此前的待查项：35 回合里 11 个正戏回合整回合零工具调用（注入在场仍跳）——低事件回合模型自判"无事可报"就整段跳过，report_progress 的空数组约定形同虚设。已把流程文案改成"每一回合都要调"，配合面板快照让欠账可见；下一局继续观察。

## 工具文案的时间指称要对齐调用时机

工具在正文动笔**之前**调用时，文案里的"本回合"指向的是还没写出来的内容——GM 会照字面理解成"预报我这回合打算写的"。实测（2026-08-21 新局）：report_progress 旧文案写"上报本回合剧情中真实达成的锚点"，GM 第 4 回合开头一口气预报 3 个锚点再把内容硬塞进同回合正文去圆（"高烧筛人"只凑了半个信号），6 个必需锚点 4 回合被打掉 5 个，序幕的慢热设计被压成速通。锚点打勾**不可逆**，误报没有回滚。已改成"只报往回合已定稿正文里的事实，本回合打算写的下回合再报"。规矩：给回合前调用的工具写文案，时间词一律锚定"已经写完的正文"，不用"本回合"。

顺带：dsh 自动标题模型会把回合头注入块读进摘要（标题变成"（开始）回合流程与序幕设定"）；`session.rename` 有 supersede 语义（手动改名取代自动生成），BFF 开局即以《剧本标题》定名根治。

## 给 GM 下指令：机械规则 > 判断规则

要 GM 稳定做一件事，就写成**没有判断余地的固定流程**，别写成"该做的时候就做"。

实测教训：机制工具上线时，提示词写的是"凡是本回合真实发生了变化的数值都要记录"，并列了触发情形。结果 GM 在正文里明明写出了"虎口发麻""蹭破的口子""她碰了碰你的伤口"——每一项都对应一个该动的数值，它完全意识到了，却一次工具都没调，因为它可以合理地判断"这点消耗不值一提"。改成"每回合第一个动作就是调用工具，没有变化传空数组"之后，立刻稳定生效。

同源的例子：行动块的输出契约靠"每一回合都必须如此结尾，无一例外"+ 紧邻生成点才稳住；强调标记从"宁缺毋滥"改成按内容类型识别（新专有名词/关键物品/转折点，每回合 2–4 处）才有输出；体力回升从"通常回 20–40"改成"任何喘息至少 +10、睡一觉回到 85 以上"才真的回得上来。

**规律**：凡是留给 GM 判断"够不够格"的规则，它一律选择不做。给类型、给数字、给固定流程。

## 机制引擎（packages/mechanics）

- **本地插件加载已验证可行**：dsh 能按绝对路径加载本仓库的插件，且**直接吃 `.ts` 源码，无需构建步骤**，`ctx.tools` 等服务正常注入。
- `--patch` 传入的覆盖层**不支持 `!!js` 动态求值**（只有 profile 目录内的 cordis.yml / cordis.patch.yml 支持），跨机器的路径差异要由剧本编译器在生成 preset 时写死。
- 机制状态必须编码进 `tool/result.meta` 再由 projection 折叠：dsh 有一份 codegen 的已知事件白名单，外部插件自定义的事件类型会让**存档重新加载时被直接拒绝**。修订事件同理走 tool/result.meta。
- **工具输出必须是无损 JSON**：返回对象里出现值为 `undefined` 的键，dsh 直接以 "value is not lossless JSON" 拒绝整个输出（实测 revise_setting 连败四次）。可选字段一律整个省略，不写 `x: undefined`。
- 写状态的工具一律不要声明 `isConcurrencySafe`（保持独占），只有纯读工具可以开并行。

## 关键事实（环境查不到的）

- **新建会话的日志不是空的**：dsh 会先写 `permission/preset`、`sandbox/mode`、`approval/policy` 三个配置事件。判断"这个会话开始过没有"要看有没有 `turn/start`，用 `events.length === 0` 会永远判为"已开始"，开场消息发不出去、界面卡在开场态。
- 剧本详情接口必须剥掉 `world.hidden_truths` 与 `cast[].secret`——那些只属于 GM 提示词，下发前端等于剧透。

- dsh 全家锁死 `0.1.0-rc.7`。官方明示 rc 版有破坏性变更、会话格式无兼容承诺——升级 dsh 是独立项目，需先在 `runtime/dsh-home` 副本上验证旧存档可读，不随手升。
- dsh 网关只信任 loopback（127.0.0.1:3090），无认证；BFF（8790）是唯一对外入口，部署时两进程必须同容器。
- `/api` 请求/事件契约以 `node_modules/@deepseek-ai/dsh-host-apiproxy` 的 `src/api/*.ts` 为准，不凭记忆写接口。会话事件信封是 `{type, seq, time, data}`。
- `DSH_HOME=runtime/dsh-home`（gitignored）：会话存档、凭据、编译产出的剧本 preset（`.agent-presets/<id>/`）都在里面，删除即丢档。
- 剧本 preset 目录由平台代码直接写文件系统生成（官方 agentPreset RPC 不支持写自定义组合）；preset 发现无缓存，新目录立即可用，但已开会话锁定其创建时的那一代。
- **dsh 对已知会话有内存记忆**：物理删除会话目录后 `session.list` 仍返回它（单存档时代被"列表只取最新"掩盖）。BFF 的存档列表、工坊定位、剧本在用检查一律按磁盘目录实存过滤。同 id 恢复备份目录后 list/history/投影均正常（实测含生成中被删的会话）。
- **改插件代码或剧本机制定义后必须重启 dsh 进程再验证**："无缓存"只覆盖 preset 目录发现；插件模块与投影注册（含闭包里的 defs）活在进程里，旧进程会用旧 defs 渲染新会话的投影——实测新会话拿到 17 条资源的组合，投影却按旧 7 条渲染。
- **本地 E2E 前先清干净遗留进程**：`--watch` 模式的 BFF 不会因端口被占而退出，会留成僵尸 watcher 与后来者抢 31415；隔夜的 dsh 进程则按旧插件闭包渲染投影。实测一台机上积了 3 个旧 BFF watcher + 1 个隔夜 dsh，E2E 第一轮拿到的是**另一部剧本**的面板数据。验证顺序：`pkill -f "watch src/index.ts"`、`pkill -f "dsh/lib/bin.js web"`，再起新进程。
- **回合首字延迟 1–55 秒是结构性的**（2026-08-20 逐回合实测）：推理阶段（flash 模型每回合先思考，长的 30 秒+）+ 工具轮（每个工具调用是一次完整模型往返）都发生在正文首字之前；纯闲聊回合 1 秒即出。前端已把阶段可见化（构思中/结算数值/清点物品）。要压延迟只能减少工具轮或换模型，不是 bug。

- **前端事件流靠"重连即重拉"自愈**（apps/web/src/stream.ts + fold.ts planResume）：切后台/锁屏/网络切换会让 SSE 在服务端已断而客户端仍显示 OPEN，漏掉的 turn/end 永远不会再来（实测：界面停在切出前画面、秒表一直跑）。对策是回前台/pageshow/online 一律重建连接，每次连接建立后重拉 history 按 seq 对齐本地状态；BFF 心跳是具名 `event: ping`（SSE 注释到不了页面脚本），前端 60s 没帧就重连——别把心跳改回注释。
- **hash 路由的刷新恢复必须用模块加载时捕获的初始 hash**：hash 同步 effect 先于恢复 effect 执行，挂载瞬间就把 `#/play` 改写成 `#/library`，现读 location.hash 只能读到改写后的值（刷新永远退回剧本库的根因）；恢复完成前同步 effect 保持沉默。

## 剧本发布防护（GM 改剧本不能改坏）

- 整读改写（read_story → 模型复述全文 → publish_story）最大的事故面是**复述时静默丢内容**。publishStory 两道防线：①覆盖发布先把旧正式版快照进 `scenarios/<id>/versions/`（保留最近 10 版，详情页可回滚，回滚本身也留档）；②缩水防线——幕/锚点/人物/资源/规则任一变少、或全文字符缩水超两成，默认拒绝并回报两版结构指纹，GM 需逐项核对后带 `confirm_shrink: true` 重发。玩家手动导入不走缩水防线（`{force:true}`），留档照常。
- 玩家剧本 preset 只挂机制工具白名单，一律不挂 bash/fs 等执行类工具（preset = shell 级权限）。
