# TaleForge

一台**剧本机器**：底座只保证结构永不坏，一切能力（机制、工艺）都是货架上的可选件，剧本用"声明选件 + 设定数据"换来一局稳定成立的游戏。基于 DeepSeek Harness（dsh）作 AI 运行时内核；玩家前端自写且固定，所有剧本共用同一 UI。单人自用，Docker 部署。

## 平台定义（2026-08-19 拷问定稿，覆盖 08-18 版；不再返工）

三层结构与归因规则：

- **底座**（平台保证，坏了就是平台 bug）：输出契约、机制与进度的固定流程、受控 Markdown 语法边界、数值裁决代码权威、转幕与结局代码判定、戏内铁律与场外协议。
- **货架**（平台提供，剧本自由选用；上新货不改剧本格式、不改前端框架）：机制模块 v1 四件套（判定/资源条/属性表/物品栏）+ 工艺模块（standard/shuang/harem/hardcore）。结构化战斗（先攻/回合/敌人实体）不做。机制 UI 是固定通用组件集：状态侧栏 + 叙事流内结构化卡片。
- **剧本**（绝对自由）：设定数据 + 显式声明选件 + 自带工艺文本（无条数上限）。**无隐藏默认**——不声明就没有；剧本里写了什么，GM 就背了什么。
- **归因**：结构坏 = 平台 bug；味道差 = 剧本的声明或自带文本；货架件本身不行 = 平台库 bug。

判定分工（Rpgforge 24 轮收敛出的分工表，照走，不设第三个裁判）：

- 能机械判定的归**代码权威**：数值裁决、转幕、结局。GM 只承接叙事，失败只能"否，但…"缓冲，不能翻案。
- 语义判定由 **GM 本人在固定流程里表态**（它是剧情的作者，不存在更知情的裁判）：锚点达成走每回合强制上报；防漏报靠工具返回值每回合列出未完成锚点与完成信号 + 代码数停滞回合分档加压。
- 剧情合理性与成败裁量是 GM 本职；剧情总结交 dsh 自带压缩，结构状态（数值/进度）每回合随工具返回值回注，不依赖总结存活。

修订机制（设定中途可改）：

- 一切剧本声明都只是**初始种子**：修订是事件（走 tool/result.meta），折叠出"现行有效设定"，每回合回注 GM，效力高于剧本原文。进度/机制投影从设计起就按"可修订"建，不做成编译期写死。
- 修订只对未来生效；想改已发生的剧情 = fork 回到那个回合。修订积累后可"落盘"合并回 story.json 源。
- 剧本永远只有**一个现行正式版**：编译、导出、新开局都用它；历史版本靠 git 留档，源文件不开分支。

戏内/场外：

- **戏内铁律**（底座）：正戏永不谈机制、不解释剧情安排、不跳出叙述者身份；识别到场外话时固定动作只有一行短提示指向场外，然后场景保持、重发行动块。
- **场外通道**：同会话同一个 GM，消息以【场外】开头；GM 以主持人身份答疑、接受软指令（剧情走向/人物戏份/风格）、执行结构化修订；场外回合不推进剧情、不产出行动块、不调数值工具。

多 agent 边界：多 agent 协同只进工坊（造剧本线，玩家不等待），**永不进玩家回合**——护栏 1 的正面表述。

已知弱点（如实记录，不装作已解决）：

1. GM 自报锚点是全设计唯一剩余的 LLM 信任点：虚报会被代码权威锁死。缓冲：锚点必须写具体完成信号 + fork 救档。F4 验收专测此处。
2. 味道层无护栏是设计而非缺陷：平台验收通过 ≠ 游戏好玩，后者靠内容线自己挣。
3. 验收整局是抽样烟测；长期保障靠被动观测逐局累积。
4. 契约用四件同形状机制定的，将来异形机制可能需要契约 v2，不追求一次定终身。

## 地基计划（F1–F6，当前阶段）

- **F1 契约定稿**：story 格式升 v1（craft 段显式声明、锚点完成信号）、persona 底座收缩 + 工艺模块化、创作说明书（兼平台能力契约）。F1 完成即交付《吞噬进化》重制版详尽初稿 + 说明书——整个剧本文件夹即外发包，用户交外部 AI 打磨，两线并行。
- **F2 幕进度 + 结局**：进度上报工具（与 adjust_resources 同一条机械规则：每回合必调、无变化传空）、投影折叠、压力分档、剧终卡与重新开始；场外 UI 入口；修订工具（设定类）。
- **F3 机制三件补齐**：判定、属性表、物品栏，复制资源条已打通的链路；机制定义可修订。
- **F4 验收**：被动结构观测（纯函数逐回合检查，只写日志，不做面板）+ 存档折叠回归 + 基准剧本（刻意平实、覆盖全货架）本地整局跑通：≥2 次转幕、结局出现、四机制各被触发、跨一次上下文压缩。
- **F5 工坊**：访谈→生成→编译→可玩闭环；修订落盘；多 agent 协同在此使用。
- **F6 部署**：服务器一次性切新版，旧档弃用（用户已授权）。

地基期间服务器与现役存档原封不动，开发验证全在本地 dsh。"先打地基"限定为玩家运行时能力，与护栏 4 不冲突——上次死于给作者造工具，这次补的是玩家脚下的地板。

## 单存档（当前阶段的产品约束）

平台同时只保留一个存档：BFF 只暴露最近一个玩过的存档，开新局时物理删除其他会话日志（dsh 没有删除 RPC）。分支功能因此关闭——接口返回 `single-save-mode`，前端无入口。dsh 侧的 fork 能力仍在，等项目成型、真正做存档管理时再开放。

**在服务器上验证功能时复用同一个测试会话，或验证完立刻清理。** 曾经一次改提示词验证一轮，累积 12 个存档全留在用户的列表里，用户无从分辨哪个是哪个。

## 硬护栏（Rpgforge 教训，违反即返工）

1. 玩家等待路径上只有 GM 主循环一个 LLM 环节（含其工具调用续写步）；Director/Observer/Judge 类额外 LLM 层一律不加。
2. GM prompt 以正向工艺指令为主，硬性禁令保持个位数；输出不满意时改工艺描述，不堆规则。
3. 状态一律 id 引用 + 严格 schema + 纯 upsert；不写模糊文本匹配兜底。
4. 玩家循环好玩之前不做作者工具面板/观测面板/评测体系；调试观测用 dsh 自带 Web 工作台（loopback 直连 3090）。可视化剧本编辑器永不做。被动观测只写日志，永不长成面板。
5. 文档只留本文件 + README + 剧本创作说明书；历史与过程交给 git log。
6. 主动跑的 LLM 评估设施（judge/golden replay 类）不建——Rpgforge 实证其使用次数≈0；观测一律被动、确定性。

## 部署（唯一目标环境）

- 生产部署永远只在 **<your-host> 的 Docker** 里，路径 `/path/to/taleforge`（该机 Docker 项目的既定布局）。不部署到其他任何环境；本地只做开发与验证。
- 该机 Docker 命令需 `sudo`（ubuntu 用户不在 docker 组）。
- 对外端口 **31415**，**绑定 127.0.0.1**：BFF 与 dsh 都没有认证，公网直接暴露等于把 API key 和存档开放给所有人。远程访问走 SSH 隧道或在 lucky 上加认证的反向代理。
- 端口须低于 32768：该机临时端口范围是 32768–60999，监听端口落在其中会偶发绑定冲突。
- 容器内 dsh 与 BFF 必须同进程空间（dsh 只信任 loopback），单容器双进程由 `docker/entrypoint.sh` 拉起。
- 持久化只有一个卷：`/path/to/taleforge/data` → 容器内 `DSH_HOME`，装着全部存档、凭据与编译产出的剧本 preset。重建容器安全，删卷即丢档。
- **API Key 由 WebUI 设置页写入**，经 dsh credentials 服务落到 `data/.credentials.yaml`，热生效、随卷持久化。`.env` 里保持没有 `DEEPSEEK_API_KEY`：环境变量是只读层，一旦有非空值就遮蔽写入通道，设置页会变成只读（此时 `credentials.describe` 返回 `writable: false`）。
- 仓库私有（github.com/want7up1/taleforge）。服务器按该机惯例用只读 deploy key 拉取：`~/.ssh/<deploy-key>`，已写进仓库的 `core.sshCommand`，`git pull` 免密。
- 更新流程：本地推送 → `ssh <your-host>` → `cd /path/to/taleforge && git pull && sudo docker compose up -d --build`。依赖层有缓存，仅改应用代码时重建很快。

## GM 提示词的分层（packages/scenario-compiler/src/persona.ts）

顺序是：**底座（结构保证）→ 工艺模块（剧本声明选用）→ 剧本数据 → 输出契约 → 开局指令**。

- 分层顺序即 prefix cache 的共享范围：改底座打散全部剧本的缓存，改某个工艺模块打散选用它的剧本的。
- **输出契约必须排在最末**，紧邻生成点。它原本跟在叙事工艺后面，中间隔着工艺模块与整份剧本设定，实测模型写到结尾会漏掉行动块，玩家因此没有选项可点。已有测试锁住这个顺序，重构时别挪回去。
- 底座只写结构；一切"怎么写得好"都在工艺模块或剧本自带文本里。同类写法在多个剧本重复出现时沉淀成新模块，别各写各的。

## 工艺指令的篇幅要匹配内容占比

给 GM 的工艺指令写在哪里，GM 就在哪里发力；没写指令的部分，无论剧本设定多丰满，都会退化成流水账。

实测教训：剧本定的配比是"情感为主、丧尸是舞台"，但爽文模板里七条指令全是碾压、捧场、正反馈，关于关系与张力的一条都没有。结果是好感度数字照涨、剧情照推，玩家的评价却是"没啥成人元素，只是同行的都是女人而已"——占八成内容的部分没有任何工艺指导。

写工艺模块或剧本 craft 段前先问：这部作品的回合时间花在哪儿？指令就得压在那儿。

## 单存档模式下不能随手在服务器上验证

单存档是物理删除实现的：**新建会话会直接删掉用户正在玩的存档**。要跑端到端验证，先确认用户当前没有在玩的进度，或者征得同意；需要反复验证时在本地起 dsh，别动服务器。

## 给 GM 下指令：机械规则 > 判断规则

要 GM 稳定做一件事，就写成**没有判断余地的固定流程**，别写成"该做的时候就做"。

实测教训：机制工具上线时，提示词写的是"凡是本回合真实发生了变化的数值都要记录"，并列了触发情形。结果 GM 在正文里明明写出了"虎口发麻""蹭破的口子""她碰了碰你的伤口"——每一项都对应一个该动的数值，它完全意识到了，却一次工具都没调，因为它可以合理地判断"这点消耗不值一提"。改成"每回合第一个动作就是调用工具，没有变化传空数组"之后，立刻稳定生效。

同源的例子：行动块的输出契约靠"每一回合都必须如此结尾，无一例外"+ 紧邻生成点才稳住；强调标记从"宁缺毋滥"改成按内容类型识别（新专有名词/关键物品/转折点，每回合 2–4 处）才有输出；体力回升从"通常回 20–40"改成"任何喘息至少 +10、睡一觉回到 85 以上"才真的回得上来。

**规律**：凡是留给 GM 判断"够不够格"的规则，它一律选择不做。给类型、给数字、给固定流程。

## 机制引擎（packages/mechanics）

- **本地插件加载已验证可行**：dsh 能按绝对路径加载本仓库的插件，且**直接吃 `.ts` 源码，无需构建步骤**，`ctx.tools` 等服务正常注入。
- `--patch` 传入的覆盖层**不支持 `!!js` 动态求值**（只有 profile 目录内的 cordis.yml / cordis.patch.yml 支持），跨机器的路径差异要由剧本编译器在生成 preset 时写死。
- 机制状态必须编码进 `tool/result.meta` 再由 projection 折叠：dsh 有一份 codegen 的已知事件白名单，外部插件自定义的事件类型会让**存档重新加载时被直接拒绝**。修订事件同理走 tool/result.meta。
- 写状态的工具一律不要声明 `isConcurrencySafe`（保持独占），只有纯读工具可以开并行。

## 关键事实（环境查不到的）

- **新建会话的日志不是空的**：dsh 会先写 `permission/preset`、`sandbox/mode`、`approval/policy` 三个配置事件。判断"这个会话开始过没有"要看有没有 `turn/start`，用 `events.length === 0` 会永远判为"已开始"，开场消息发不出去、界面卡在开场态。
- 剧本详情接口必须剥掉 `world.hidden_truths` 与 `cast[].secret`——那些只属于 GM 提示词，下发前端等于剧透。

- dsh 全家锁死 `0.1.0-rc.7`。官方明示 rc 版有破坏性变更、会话格式无兼容承诺——升级 dsh 是独立项目，需先在 `runtime/dsh-home` 副本上验证旧存档可读，不随手升。
- dsh 网关只信任 loopback（127.0.0.1:3090），无认证；BFF（8790）是唯一对外入口，部署时两进程必须同容器。
- `/api` 请求/事件契约以 `node_modules/@deepseek-ai/dsh-host-apiproxy` 的 `src/api/*.ts` 为准，不凭记忆写接口。会话事件信封是 `{type, seq, time, data}`。
- `DSH_HOME=runtime/dsh-home`（gitignored）：会话存档、凭据、编译产出的剧本 preset（`.agent-presets/<id>/`）都在里面，删除即丢档。
- 剧本 preset 目录由平台代码直接写文件系统生成（官方 agentPreset RPC 不支持写自定义组合）；preset 发现无缓存，新目录立即可用，但已开会话锁定其创建时的那一代。
- **改插件代码或剧本机制定义后必须重启 dsh 进程再验证**："无缓存"只覆盖 preset 目录发现；插件模块与投影注册（含闭包里的 defs）活在进程里，旧进程会用旧 defs 渲染新会话的投影——实测新会话拿到 17 条资源的组合，投影却按旧 7 条渲染。
- 玩家剧本 preset 只挂机制工具白名单，一律不挂 bash/fs 等执行类工具（preset = shell 级权限）。
