# TaleForge 剧本创作说明书（taleforge.story.v1）

你（人或 AI）拿到的是一份 **TaleForge 剧本创作说明书**，配套一份 `story.json`（剧本本体）使用——既可以打磨现有剧本，也可以照着本说明从零写一份新剧本。TaleForge 是一个单人文字 RPG 平台——AI GM 按剧本运营一部长篇互动小说，玩家每回合从行动选项中选择或自由行动。

**产出物只有一个 `story.json` 文件。** 写完/改完交回平台导入即可，导入时自动校验（错误逐条返回）并编译上架。每个剧本永远只有一个现行正式版；不要另存副本、不要开分支版本。

## 平台与剧本的分工（先读这个，决定你该改什么）

- **平台保证结构**：每回合的行动选项块、数值的裁决与展示、幕进度追踪、结局触发、排版语法边界。这些你写不了也不用管——剧本里不要写任何关于输出格式、选项数量、数值计算方式的指令。
- **剧本负责一切内容与味道**：世界观、人物、剧情结构、数值的语义、写作风格。GM 写得好不好看，取决于你在这份文件里声明了什么——**没有隐藏默认，你不写的东西就不存在**。

## story.json 逐字段说明

### 顶层

| 字段 | 说明 | 可改？ |
|---|---|---|
| `format` | 固定 `"taleforge.story.v1"` | 不可改 |
| `id` | 剧本唯一标识，`story-` 开头的 kebab-case | 不可改 |
| `title` / `tagline` | 标题与一句话卖点，玩家在剧本库里看到的门面 | 可改 |

### world（世界）

- `overview`：世界观总纲，直接进 GM 的脑子。写成有画面的散文，不写设定清单；GM 只知道这里写了的东西。
- `tone`：基调关键词数组。
- `hidden_truths[]`：`{id, text}`。**只有 GM 知道的底层真相**，玩家永远看不到原文；GM 靠它保持一致性、埋线索，但被禁止直接说破。每条 id 是 kebab-case。

### protagonist（主角）与 cast（出场人物）

- `protagonist.voice`：叙述人称与文风声明（本剧本用第二人称「你」）。
- `cast[]`：`{id, name, identity, secret}`。`identity` 是人物的可见设定；`secret` 是**只进 GM 提示词的暗线**，玩家界面永远不显示——人物的秘密、动机、结局钩子都写在这里。打磨人物主要就是打磨 `identity` 的鲜活度和 `secret` 的戏剧潜力。

### opening（开场）

`scene` 是第一回合的画面，`hook` 是第一回合必须收在的钩子。玩家的第一印象全在这两段，值得反复打磨。

### acts（幕结构）

每幕 `{id, title, objective, anchors[], forbidden_reveals[]}`：

- `anchors[]`：`{id, text, required, signal}`。锚点是本幕必须发生的剧情事件；`required: true` 的全部达成后才会转幕。
- `reminder`（可选，≤600 字）：**分幕贴身提醒**——本幕进行期间替换 `craft.reminder` 随每回合注入生成点旁。给世界状态或文风随剧情阶段变化的剧本用：爆发前的平常世界、爆发后的荒野、觉醒后的碾压期各写各的一段，只有当前幕那段会被注入，未到的幕玩家与 GM 都看不到（天然防剧透）。没写 `reminder` 的幕自动回落到 `craft.reminder`。
- **`signal`（完成信号）是本格式最重要的字段之一**：一句可观察、少歧义的剧情事实，GM 每回合对照它判断锚点是否达成。写「主角吞噬晶核并感到力量变化」这种可核对的句子，不写「主角变强了」这种模糊话。信号写得含糊，游戏就会卡幕或跳幕。
- `forbidden_reveals[]`：本幕禁止直接说破的内容（通常引用 hidden_truths / secrets 的主题）。到了可以揭露的幕，就从列表里去掉。

### mechanics（机制货架，四件套按需选调）

机制段有五个模块，**声明哪个游戏里就有哪个**，各自独立可选（progression 依赖 attributes）；全部省略即纯叙事。

**resources（资源条）**——每条 `{id, label, group, min, max, initial, floor?, maxStep, guidance}`：

- `group`：`affinity`（人物好感）/ `self`（主角自身）/ `world`（队伍与环境）。决定侧栏分组。分组标题可用 `mechanics.groups` 自定义（如 `{"affinity": "红颜", "world": "基地"}`）。
- `display`（选位，平台枚举）：`strip`=顶栏常驻（给玩家每回合都该看见的核心数值）、`panel`=只进卷宗面板、`hidden`=只记账不展示（GM 可见玩家不可见——倒计时、暗中变化的数值用它；注意这是界面约定，不是防作弊）。缺省：`self` 组进顶栏，其余进面板。**位置和组件由平台枚举，剧本只做选位**——没有自定义组件或布局。
- `revealWith`（防剧透门控）：绑定一个 cast id，该人物在正文中出场之前，这条资源连同其结算记录对玩家不可见。**凡是绑定后续出场人物的好感/专属数值，一律要加**——未出场人物的数值条本身就是剧透。（人物出场判定与卷宗人物列表、未来幕打码由平台自动处理，剧本无需声明。）
- `maxStep`：单回合最大变动，防止数值失去意义；`floor`：跌不破的下限。
- **`guidance` 是数值的灵魂**，直接进 GM 提示词。必须包含三件事：①什么事件导致增减、**给具体数字**（「战斗 -10～20」，不是「战斗会消耗」）；②各数值区段的含义（30 和 70 差在哪）；③需要 GM 在正文里怎么体现。
- **写 guidance 的铁律：机械规则，不写判断规则。** 实测教训：写「该恢复的时候恢复」，GM 永远选择不恢复；写「任何喘息回合至少 +10、睡一觉回到 85 以上」，立刻生效。给类型、给数字、给固定流程，永远不要给 GM 留「够不够格」的判断余地。

**attributes（属性表）**——每条 `{id, label, initial, min?, max?, maxStep?, guidance}`（区间默认 0–20，单步默认 1）：变动稀少的能力值，界面显示为表格；判定自动引用作修正。`guidance` 写清这条属性衡量什么、**什么级别的剧情事件才配让它 +1**。

**checks（判定/骰子）**——`{die?, guidance}`（骰型默认 `d20`，可选 `d100`/`2d6`）：启用后，成败不确定的行动由代码掷骰裁决，GM 无权翻案（d20 天然 20/1 为大成功/大失败）。`guidance` 必须写成机械规则：**什么类型的行动必须掷**（列类型，别写「关键时刻」）+ **难度分几档、各是多少**（如「普通 12 / 困难 15 / 极难 18」）。

**inventory（物品栏）**——`{guidance, initial?}`：物品一律 id 引用、纯 upsert。`guidance` 写清**什么算需要入账的物品、什么不算**（例：武器弹药药品入账，一次性杂物不入），以及**入账粒度**——成批物资按种类分条给数量，不打包成一条「物资」；`initial` 是开局物品 `[{id, name, qty?, note?}]`。平台每回合会把物品栏与全部数值的即时快照贴到 GM 生成点旁，正文账实相符靠这份快照兜底。

**progression（经验与等级）**——`{label?, guidance, maxStep, thresholds, pointsPerLevel, display?}`，需同时声明 attributes：GM 按 `guidance` 上报经验（同资源条，代码裁单次上限 `maxStep`）；`thresholds` 是升到 2、3、…级各需累计的经验，严格递增，表长 + 1 为满级（经验封顶在最后一档）；每升一级由代码发放 `pointsPerLevel` 点属性点，**玩家自己在卷宗里加到属性上**（随下一步行动落账，GM 无权代为分配），属性上限沿用 `attributes[].max`。`guidance` 同样只写机械规则：什么事件给多少（击杀 +10～30、达成锚点 +50、纯对话 0），以及升级在剧情里怎么体现。开启后属性主要靠加点成长，`adjust_attributes` 只留给属性 guidance 明确写出的剧情奖励；想要"升级回满体力"这类联动，写进对应资源的 guidance。`display`：`strip` 顶栏常驻（缺省）/ `panel` 只进卷宗。

**中途修改**：游戏进行中，玩家可在场外让 GM 修订资源与属性的语义和边界（改 guidance/min/max/maxStep 等，立即生效）；但**增删条目**做不到——那要改这份文件然后开新局。所以条目清单值得在这里一次定对。

### craft（工艺声明）

- `modules`：从平台工艺货架选用的模块，按声明顺序生效。**不可改动模块名**，可调整选用与顺序。货架目录：
  - `standard` —— 通用叙事工艺：承接、演而非讲、对白推动、节奏呼吸、强调标记的用法。几乎所有剧本都该带。
  - `shuang` —— 爽文向：出手碾压、旁人捧场、每回合有收获、阻力只做衬托。
  - `harem` —— 关系与张力：距离感写在身体上、独处场景、女主之间的戏、欲望与越界的写法。强度中立：写多深由 rating 决定，模块不设上下限。
  - `hardcore` —— 硬核向：代价与失败是好戏、「是但/否但」裁定、危险靠情境演。与 `shuang` 气质相反，一般不同时选。
- `rating`：**内容强度的唯一契约**——GM 写到什么程度以此为准，工艺模块永不设强度上限或下限。想要多直白就在这里声明多直白，想要克制同样在这里声明。
- `rules[]`：**本剧本专属的工艺指令，无条数上限**——这是你自由发挥的主要空间。写正向的工艺描述（「要怎么写」），不堆禁令。
- `reminder`（可选，≤600 字）：**贴身提醒**——平台会把这段文本随每一个正戏回合注入到 GM 生成点旁边，效力高于 rating 与 rules 在设定里的位置。长局实测：几十回合后正文自己的文风先例会压过设定深处的声明（rating 照样被漂移掉）；把「每回合必须坚持、且容易被漂移」的要求写在这里——文风尺度、词汇纪律、节奏铁律，各一两句，越短越有力。剧情内容不要写进来。文风随剧情阶段变化的剧本，用 `acts[].reminder` 分幕覆盖（见幕结构一节）。

## 打磨时的四条经验法则（前一版用真实游玩换来的）

1. **工艺指令的篇幅要匹配内容占比。** GM 只在有指令的地方发力：上一版设定说「情感为主」，但工艺指令全在写战斗，结果玩家评价「没啥成人元素」。这部作品八成的戏在人身上，`rules` 就要八成压在人身上——现在的配比规则就是这么来的，删改时保住这个原则。
2. **数值语义给数字，不给形容词。**（见上文 guidance 铁律。）
3. **完成信号可核对。** 每个 anchor 的 `signal` 都要能回答「这回合发生了吗——是/否」。
4. **秘密放对地方。** 玩家不该提前知道的一切，只能放 `hidden_truths` 和 `cast[].secret`；写进 `world.overview` 或 `identity` 的内容等于当场公开。

## 修改边界与交付标准

- 保持 JSON 语法合法（注意引号与逗号；字符串内换行用 `\n`）。
- 覆盖发布自动留档最近 10 版，剧本详情页可回滚；发布若被缩水防线拦下（新版比现行版少了幕/锚点/人物/资源/规则或全文明显变短），先逐项核对少掉的内容是否确为要求的删减，确认后才带确认参数重发。
- 不可改：`format`、`id`、craft 模块名、字段结构（不增删字段、不改字段名、id 类字段保持 kebab-case）。
- 其余一切内容（文案、人物、幕与锚点的数量与内容、资源的数量与语义、rules）都可以自由增删改写。
- 交付标准：每个改动过的字段读起来都比原稿更具体、更有画面；没有一条 guidance 是形容词式的；没有一个 signal 是不可核对的。
